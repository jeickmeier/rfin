// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { InstrumentConventions } from "./InstrumentConventions";

/**
 * Market quote for credit instruments.
 */
export type CreditQuote = { "cds_par_spread": { 
/**
 * Unique identifier for the quote.
 */
id: string, 
/**
 * Reference entity name.
 */
entity: string, 
/**
 * Convention key (currency + doc clause).
 */
convention: string, 
/**
 * Maturity pillar.
 */
pillar: string, 
/**
 * Par spread in basis points (e.g. 100.0).
 */
spread_bp: number, 
/**
 * Recovery rate assumption (e.g. 0.40).
 */
recovery_rate: number, } } | { "cds_upfront": { 
/**
 * Unique identifier for the quote.
 */
id: string, 
/**
 * Reference entity name.
 */
entity: string, 
/**
 * Convention key.
 */
convention: string, 
/**
 * Maturity pillar.
 */
pillar: string, 
/**
 * Running spread in basis points (e.g. 100.0 or 500.0).
 */
running_spread_bp: number, 
/**
 * Upfront payment percentage of notional (e.g. 0.01 for 1%).
 */
upfront_pct: number, 
/**
 * Recovery rate assumption.
 */
recovery_rate: number, } } | { "c_d_s_tranche": { 
/**
 * Unique identifier.
 */
id: string, 
/**
 * Index identifier (e.g. CDX.NA.HY).
 */
index: string, 
/**
 * Attachment point (decimal, e.g. 0.03).
 */
attachment: number, 
/**
 * Detachment point (decimal, e.g. 0.07).
 */
detachment: number, 
/**
 * Maturity date.
 */
maturity: string, 
/**
 * Upfront payment percentage.
 */
upfront_pct: number, 
/**
 * Running spread (bps).
 */
running_spread_bp: number, 
/**
 * Legacy conventions (needed for base correlation logic).
 */
conventions: InstrumentConventions, } };
