//! Golden tests using the unified framework.
//!
//! This module contains tests that load expected values from JSON fixtures
//! and compare them against computed results using the `finstack_core::golden` framework.

mod variance_tests;
