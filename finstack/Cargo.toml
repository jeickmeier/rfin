[package]
name = "finstack"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["core"]
core = ["dep:finstack_core"]
statements = ["core", "dep:finstack_statements"]
valuations = ["core", "dep:finstack_valuations"]
analysis = ["statements", "valuations", "dep:finstack_analysis"]
scenarios = ["statements", "valuations", "dep:finstack_scenarios"]
portfolio = ["statements", "valuations", "scenarios", "dep:finstack_portfolio"]
io = ["dep:finstack_io"]
all = ["statements", "valuations", "analysis", "scenarios", "portfolio", "io"]

[dependencies]
finstack_core = { package = "finstack-core", path = "core", optional = true }
finstack_statements = { package = "finstack-statements", path = "statements", optional = true }
finstack_valuations = { package = "finstack-valuations", path = "valuations", optional = true }
finstack_analysis = { package = "finstack-analysis", path = "analysis", optional = true }
finstack_scenarios = { package = "finstack-scenarios", path = "scenarios", optional = true }
finstack_portfolio = { package = "finstack-portfolio", path = "portfolio", optional = true }
finstack_io = { package = "finstack-io", path = "io", optional = true }


