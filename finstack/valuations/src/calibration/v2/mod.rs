/// Adapters mapping API to domain execution.
pub mod adapters;
/// API layer for calibration v2 (schema, engine, execution).
pub mod api;
/// Domain logic for calibration (quotes, pricing, solver).
pub mod domain;
