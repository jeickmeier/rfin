//! Metrics for convertible bonds (stub).
//!
//! Registers placeholder metrics for `ConvertibleBond`. Real calculators will
//! be added in subsequent iterations (e.g., parity, conversion value, premium).

use crate::metrics::MetricRegistry;

/// Register convertible bond metrics into the registry.
pub fn register_convertible_metrics(registry: &mut MetricRegistry) {
    let _ = registry; // no-op for now
}


